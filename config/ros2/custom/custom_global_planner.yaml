straight_line_global_planner:
  ros__parameters:
    use_sim_time: true
    odometry_topic: /rmf/odom
    goal_topic: /goal_pose
    path_topic: /gbplanner_path
    frame_id: map
    replan_interval: 2.0
    segment_length: 1.0
